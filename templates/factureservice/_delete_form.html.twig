<form method="post" action="{{ path('app_factureservice_delete', {'idfactureservice': factureservice.idfactureservice}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ factureservice.idfactureservice) }}">
    <button class="btn">Delete</button>
</form>
