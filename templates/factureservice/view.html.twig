{% extends 'base-front-passcmd.html.twig' %}

{% block title %}
    <div class="section-title">
        <h2>{{'Votre commande est enregistrée avec succès'|trans|raw}}</h2>
    </div>
{% endblock %}
{% block body %}
    <embed src="{{ asset('uploads/brochures/'~ facture.getFactureservicepdf()~'service.pdf')}}" width="1300" height="1200" type="application/pdf">

    </br>
    </br>
    </br>

    <div class="col text-center">
        <h3 style="color: #1b2658">{{ 'Scanner le code'|trans|raw }}</h3></br>
        <img src="{{asset('Front-office/img/My_PDF (1).png')}}" width="500px" />
    </div>
    </br>
    </br>
    <div class="col-lg-12 grid-margin stretch-card" >
        <div class="text-center">
            <h4>
            <a style="color: #1b2658" href="{{ path('downloadpdfservice',{'utilisateur':u.iduser,'commande':c.idcommandeservice,'ser':s.idservice}) }}">{{'Télécharger la facture'|trans|raw}}</a>
            </h4>
        </div>
    </div>

{% endblock %}
