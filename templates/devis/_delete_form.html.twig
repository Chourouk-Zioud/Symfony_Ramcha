<form method="post" action="{{ path('app_devis_delete', {'iddevis': devi.iddevis}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ devi.iddevis) }}">
    <button class="btn">Delete</button>
</form>
