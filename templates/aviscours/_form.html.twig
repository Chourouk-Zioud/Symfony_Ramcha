
<div style="width:50%;margin-top: 6rem;margin-left: auto;margin-right: auto;">
    <div class="card border border-primary">
        <div class="card-body  p-5">
            <div>{{ form_label(form.rate,"Rate") }}</div>
            <div>{{ form_widget(form.rate ,{ 'attr': {'class': 'form-control mb-2'} }) }}</div>
            <div>{{ form_errors(form.rate) }}</div>

            <div>{{ form_label(form.commentaire,"Commentaire") }}</div>
            <div>{{ form_widget(form.commentaire , { 'attr': {'class': 'form-control mb-2'} }) }}</div>
            <div>{{ form_errors(form.commentaire ) }}</div>
            <div class="d-flex justify-content-end">
                <button class="btn btn-primary">{{ button_label|default('Save') }}</button>
                <a  href="{{ path('app_aviscours_index') }}" class="btn btn-secondary ml-3" >{{ button_label|default('Return') }}</a></div></div>
        </div> </div></div>
</div></div>

</div>
{{ form_end(form) }}


