{#  {% extends 'base-front.html.twig' %}

{% block title %}Aviscours index{% endblock %}

{% block body %}
    <h1>Les Avis  </h1>

    <table class="table">
        <thead>
        <tr>

            <th>Rate</th>
            <th>Commentaire</th>
            <th>actions</th>
        </tr>
        </thead>
        <tbody>
        {% for aviscour in aviscours %}
            <tr>
                <td>{{ aviscour.rate }}</td>
                <td>{{ aviscour.commentaire }}</td>
                <td>
                    <a href="{{ path('app_aviscours_show', {'idaviscours': aviscour.idaviscours}) }}">show</a>
                    <a href="{{ path('app_aviscours_edit', {'idaviscours': aviscour.idaviscours}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="6">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('app_aviscours_new') }}">Create new</a>
{% endblock %}
#}
