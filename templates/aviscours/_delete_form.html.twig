<form method="post" action="{{ path('app_aviscours_delete', {'idaviscours': aviscour.idaviscours}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ aviscour.idaviscours) }}">
    <button class="btn">Delete</button>
</form>
