<div class="d-flex justify-content-center">

<div class="text-center">
    <div class="form-group">
        <form method="post" action="{{ path('app_commandeservice_delete', {'idcommandeservice': commandeservice.idcommandeservice}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
            <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ commandeservice.idcommandeservice) }}">
            <button class="btn btn-inverse-secondary btn-fw"><i class="ti-alert btn-icon-prepend">  </i>{{ 'Refuser la commande'|trans|raw }}</button>
        </form>
    </div>
</div>
</div>
