<div class="text-center">
    <div class="form-group">
        <form method="post" action="{{ path('app_commandeservice_eletecmdser', {'idcommandeservice': commandeservice.idcommandeservice,'a':a}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
            <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ commandeservice.idcommandeservice) }}">
            <button class="btn btn-danger">{{ 'Delete'|trans|raw }}</button>
        </form>
    </div>
</div>
