

<div style="width:50%;margin-top: 6rem;margin-left: auto;margin-right: auto;">
    <div class="card border border-primary">
        <div class="card-body  p-5">
            {{ form_start(form,{'attr' : {'novalidate' : 'novalidate'}} ) }}

            <div style="color: #0d3057" class="text-center"><strong>{{ form_label(form.libelledemande, "Libelle demande" )}}</strong></div>
            <div>{{ form_widget(form.libelledemande,{ 'attr': {'class': 'form-control mb-2'} }) }}</div>

            <div>{{ form_errors(form.libelledemande) }}</div>

            <div class="d-flex justify-content-center">
                <button class="btn btn-inverse-success btn-fw ">
                    {{ button_label|default('Ajouter'|trans|raw ) }}
                </button>

            </div>
            {{ form_end(form) }}

            <br>
            <br>
            <div class="d-flex">
                <button class="btn btn-primary btn-rounded btn-icon">
                    <a href="{{ path('app_utilisateur_index') }}" style="color: white"><i class="ti-back-left btn-icon-prepend"></i></a></button>
            </div>

        </div>
    </div>
</div>
