{% extends 'base.html.twig' %}

{% block title %}Facture{% endblock %}

{% block body %}
    <h1>Facture</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Idfacture</th>
                <td>{{ facture.idfacture }}</td>
            </tr>
            <tr>
                <th>Datefacture</th>
                <td>{{ facture.datefacture ? facture.datefacture|date('Y-m-d') : '' }}</td>
            </tr>
            <tr>
                <th>Facturepdf</th>
                <td>{{ facture.facturepdf }}</td>
            </tr>
            <tr>
                <th>Iddevis</th>
                <td>{{ facture.iddevis }}</td>
            </tr>
            <tr>
                <th>Idcommande</th>
                <td>{{ facture.idcommande }}</td>
            </tr>
            <tr>
                <th>Total</th>
                <td>{{ facture.total }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_facture_index') }}">back to list</a>

    <a href="{{ path('app_facture_edit', {'idfacture': facture.idfacture}) }}">edit</a>

    {{ include('facture/_delete_form.html.twig') }}
{% endblock %}
