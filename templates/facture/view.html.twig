{% extends 'base-front-passcmd.html.twig' %}

{% block title %}
    <div class="section-title">
        <h2>{{'Votre commande est enregistrée avec succès'|trans|raw}}</h2>
    </div>
{% endblock %}
{% block body %}

<div class="justify-content-center">
    <embed src="{{ asset('uploads/brochures/'~ facture.getFacturepdf()~'.pdf')}}" width="1300" height="1200" type="application/pdf" id="pdf">
</div>
    <br>
    <br>
    <div class="col text-center">
        <h3 style="color: #1b2658 ">{{ 'Scanner le code'|trans|raw }}</h3></br>
        <img src="{{asset('Front-office/img/My_PDF.png')}}" width="500px" />
    </div>
    </br>

    <div class="col-lg-12 grid-margin stretch-card" >

        <div class="text-center">
            <h3>{{'Ou'|trans|raw}}</h3>
        </br>
            <h4>
                <a style="color: #1b2658" href="{{ path('downloadpdf',{'utilisateur':u.iduser,'commande':c.idcommande,'art':a.idarticle}) }}">{{'Télécharger la facture'|trans|raw}}</a>
            </h4>
        </div>
    </div>

{% endblock %}
