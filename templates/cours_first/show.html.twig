{% extends 'base-backcours.html.twig' %}

{% block title %}CoursFirst{% endblock %}

{% block body %}
    <h1 class="text-center" style="color: #0d3057">
        <br> Le cours Selectionné</h1>

<div class="col-lg-12 grid-margin stretch-card">
    <div class="card">
        <div class="card-body">

            <div class="table-responsive">

    <table class="table">
        <tbody>
            <tr>
                <th style="color: #0d3057"><strong>Idfirst </strong></th>
                <td>{{ cours_first.idfirst }}</td>
            </tr>

            <tr>
                <th style="color: #0d3057"><strong>Nomcours </strong></th>
                <td>{{ cours_first.nomcours }}</td>
            </tr>
            <tr>
                <th style="color: #0d3057"><strong>Categoriecours </strong></th>
                <td>{{ cours_first.categoriecours }}</td>
            </tr>
            <tr>
                <th style="color: #0d3057"><strong>Sujetcours </strong></th>
                <td>{{ cours_first.sujetcours }}</td>
            </tr>
            <tr>
                <th style="color: #0d3057"><strong>Niveaucours </strong></th>
                <td>{{ cours_first.niveaucours }}</td>
            </tr>
        </tbody>
    </table>
                <div class="d-flex justify-content-end">
                    <a href="{{ path('app_cours_first_edit', {'idfirst': cours_first.idfirst}) }}" class="btn btn-outline-primary">{{ button_label|default('Modifier') }}</a>
                    <a  href="{{ path('app_cours_first_index', {'idfirst': cours_first.idfirst}) }}" class="btn btn-outline-secondary ml-3" >{{ button_label|default('Return') }}</a></div></div>
        </div> </div></div>
            </div>
        </div>
    </div>
</div>


{% endblock %}
